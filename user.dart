class User {
  String name, email, password, datereg, status;
  User(
      {required this.name,
      required this.email,
      required this.password,
      required this.datereg,
      required this.status});
}